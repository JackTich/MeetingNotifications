<?xml version="1.0" encoding="utf-8"?>
<androidx.constraintlayout.widget.ConstraintLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    xmlns:app="http://schemas.android.com/apk/res-auto">
    <LinearLayout
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:id="@+id/containerHeaderEtForm"
        android:orientation="horizontal"
        app:layout_constraintTop_toTopOf="parent"
        app:layout_constraintLeft_toLeftOf="parent">
        <TextView
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:id="@+id/tvTitleEtForm"
            android:textSize="@dimen/small_text_size"
            android:textColor="@color/darkGrey"
            />
        <TextView
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:id="@+id/imgIsEtFormRequired"
            android:visibility="gone"
            android:textSize="@dimen/small_text_size"
            android:layout_marginStart="5dp"
            android:textColor="@color/redDark"
            android:text="@string/char_star"/>
    </LinearLayout>
    <EditText
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:lines="1"
        android:layout_marginTop="10dp"
        android:id="@+id/etFormMeeting"
        android:textSize="@dimen/standard_text_size"
        android:textColor="@color/primaryText"
        app:layout_constraintTop_toBottomOf="@+id/containerHeaderEtForm" />
    <TextView
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:maxLines="2"
        android:layout_marginTop="10dp"
        android:id="@+id/tvErrorEtForm"
        app:layout_constraintTop_toBottomOf="@+id/etFormMeeting"
        android:textSize="@dimen/small_text_size"
        android:textColor="@color/redDark"
        android:visibility="gone"
        />
</androidx.constraintlayout.widget.ConstraintLayout>